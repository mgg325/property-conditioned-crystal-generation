# External inputs

This directory is intentionally empty. Do not add external datasets, model weights, or reference databases to the public repository without confirming their terms.

The released verification and figure data use files under `data/`. Training, generation, and new relaxation runs also require the external software and assets listed in `THIRD_PARTY.md`.
